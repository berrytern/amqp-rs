[workspace]
members = [
    "core",
    "bindings/python",
    "bindings/node"
]
resolver = "2"

[patch.crates-io]
amqp-client-rust = { path = "../amqp-client-rust/amqp-client-rust" }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
opt-level = 3